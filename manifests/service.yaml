apiVersion: v1
kind: Service
metadata:
  name: sensor-view
  namespace: sensor-view
spec:
  type: NodePort
  selector:
    app: sensor-view
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 32225
